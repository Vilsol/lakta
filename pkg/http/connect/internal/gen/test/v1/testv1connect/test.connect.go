// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: test/v1/test.proto

package testv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/Vilsol/lakta/pkg/http/connect/internal/gen/test/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// EchoServiceName is the fully-qualified name of the EchoService service.
	EchoServiceName = "test.v1.EchoService"
	// ValidateServiceName is the fully-qualified name of the ValidateService service.
	ValidateServiceName = "test.v1.ValidateService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// EchoServiceEchoProcedure is the fully-qualified name of the EchoService's Echo RPC.
	EchoServiceEchoProcedure = "/test.v1.EchoService/Echo"
	// ValidateServiceCheckProcedure is the fully-qualified name of the ValidateService's Check RPC.
	ValidateServiceCheckProcedure = "/test.v1.ValidateService/Check"
)

// EchoServiceClient is a client for the test.v1.EchoService service.
type EchoServiceClient interface {
	Echo(context.Context, *connect.Request[v1.EchoRequest]) (*connect.Response[v1.EchoResponse], error)
}

// NewEchoServiceClient constructs a client for the test.v1.EchoService service. By default, it uses
// the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewEchoServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) EchoServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	echoServiceMethods := v1.File_test_v1_test_proto.Services().ByName("EchoService").Methods()
	return &echoServiceClient{
		echo: connect.NewClient[v1.EchoRequest, v1.EchoResponse](
			httpClient,
			baseURL+EchoServiceEchoProcedure,
			connect.WithSchema(echoServiceMethods.ByName("Echo")),
			connect.WithClientOptions(opts...),
		),
	}
}

// echoServiceClient implements EchoServiceClient.
type echoServiceClient struct {
	echo *connect.Client[v1.EchoRequest, v1.EchoResponse]
}

// Echo calls test.v1.EchoService.Echo.
func (c *echoServiceClient) Echo(ctx context.Context, req *connect.Request[v1.EchoRequest]) (*connect.Response[v1.EchoResponse], error) {
	return c.echo.CallUnary(ctx, req)
}

// EchoServiceHandler is an implementation of the test.v1.EchoService service.
type EchoServiceHandler interface {
	Echo(context.Context, *connect.Request[v1.EchoRequest]) (*connect.Response[v1.EchoResponse], error)
}

// NewEchoServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewEchoServiceHandler(svc EchoServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	echoServiceMethods := v1.File_test_v1_test_proto.Services().ByName("EchoService").Methods()
	echoServiceEchoHandler := connect.NewUnaryHandler(
		EchoServiceEchoProcedure,
		svc.Echo,
		connect.WithSchema(echoServiceMethods.ByName("Echo")),
		connect.WithHandlerOptions(opts...),
	)
	return "/test.v1.EchoService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case EchoServiceEchoProcedure:
			echoServiceEchoHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedEchoServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedEchoServiceHandler struct{}

func (UnimplementedEchoServiceHandler) Echo(context.Context, *connect.Request[v1.EchoRequest]) (*connect.Response[v1.EchoResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("test.v1.EchoService.Echo is not implemented"))
}

// ValidateServiceClient is a client for the test.v1.ValidateService service.
type ValidateServiceClient interface {
	Check(context.Context, *connect.Request[v1.CheckRequest]) (*connect.Response[v1.CheckResponse], error)
}

// NewValidateServiceClient constructs a client for the test.v1.ValidateService service. By default,
// it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and
// sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC()
// or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewValidateServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ValidateServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	validateServiceMethods := v1.File_test_v1_test_proto.Services().ByName("ValidateService").Methods()
	return &validateServiceClient{
		check: connect.NewClient[v1.CheckRequest, v1.CheckResponse](
			httpClient,
			baseURL+ValidateServiceCheckProcedure,
			connect.WithSchema(validateServiceMethods.ByName("Check")),
			connect.WithClientOptions(opts...),
		),
	}
}

// validateServiceClient implements ValidateServiceClient.
type validateServiceClient struct {
	check *connect.Client[v1.CheckRequest, v1.CheckResponse]
}

// Check calls test.v1.ValidateService.Check.
func (c *validateServiceClient) Check(ctx context.Context, req *connect.Request[v1.CheckRequest]) (*connect.Response[v1.CheckResponse], error) {
	return c.check.CallUnary(ctx, req)
}

// ValidateServiceHandler is an implementation of the test.v1.ValidateService service.
type ValidateServiceHandler interface {
	Check(context.Context, *connect.Request[v1.CheckRequest]) (*connect.Response[v1.CheckResponse], error)
}

// NewValidateServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewValidateServiceHandler(svc ValidateServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	validateServiceMethods := v1.File_test_v1_test_proto.Services().ByName("ValidateService").Methods()
	validateServiceCheckHandler := connect.NewUnaryHandler(
		ValidateServiceCheckProcedure,
		svc.Check,
		connect.WithSchema(validateServiceMethods.ByName("Check")),
		connect.WithHandlerOptions(opts...),
	)
	return "/test.v1.ValidateService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ValidateServiceCheckProcedure:
			validateServiceCheckHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedValidateServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedValidateServiceHandler struct{}

func (UnimplementedValidateServiceHandler) Check(context.Context, *connect.Request[v1.CheckRequest]) (*connect.Response[v1.CheckResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("test.v1.ValidateService.Check is not implemented"))
}
